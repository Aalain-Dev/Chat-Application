1. Learned about how to initialize the TypeScript and need to do the changes accordingly in the tsconfig.json file. 
2. Learned about the NPM package call concurrently that is required to run two commands in one Rather than having two separate terminals 
3. Learn about assert queue and send to queue in RabbitMQ. A assert queue is basically that we are creating something to send Means a way that we can send 
Send to queue is basically that it is scheduled to be sent. 
4. learned how can a docker can be used to pull 
5. Learned the rabbitmq can be run locally by pulling through docker